{
  "name": "tingting",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mobile": "npm run build:shared && npm run start --workspace=mobile",
    "typecheck": "npm run typecheck --workspace=mobile",
    "build:shared": "npm run build --workspace=@tingting/shared",
    "build:api": "npm run build:shared && npm run build --workspace=@tingting/api",
    "start:api": "node -r ./scripts/load-dotenv.cjs apps/api/dist/index.js",
    "build:web": "npm run build:shared && npm run export:web --workspace=mobile && node -r ./scripts/load-dotenv.cjs scripts/write-web-config.js",
    "start:web": "node -r ./scripts/load-dotenv.cjs scripts/serve-web.js",
    "sync:env": "powershell -ExecutionPolicy Bypass -File scripts/sync-railway-env.ps1"
  },
  "devDependencies": {
    "dotenv": "^16.4.7",
    "serve": "^14.2.4"
  },
  "dependencies": {
    "expo-router": "~56.2.11"
  }
}
